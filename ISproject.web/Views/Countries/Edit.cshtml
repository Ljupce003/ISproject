@model ISproject.Domain.Models.Country

@{
    ViewData["Title"] = "Edit Country";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="card-header bg-warning py-3 px-4 border-0">
                    <h4 class="mb-0 fw-bold text-dark"><i class="bi bi-pencil-square me-2"></i>Update Country</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3"></div>
                        <input type="hidden" asp-for="Id" />

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-bold text-secondary">Country Name</label>
                            <input asp-for="Name" class="form-control border-2 shadow-sm" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Code" class="form-label fw-bold text-secondary">ISO Code</label>
                            <input asp-for="Code" class="form-control border-2 shadow-sm fw-bold text-primary uppercase" maxlength="2" />
                            <span asp-validation-for="Code" class="text-danger small"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning px-4 rounded-pill fw-bold shadow-sm">
                                <i class="bi bi-save"></i> Save Changes
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary px-4 rounded-pill">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>